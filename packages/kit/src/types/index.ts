export type Variant = 'primary' | 'secondary' | 'danger';
export type IconSide = 'right' | 'left';
export type Size = 'lg' | 'sm';

export type InputModelModifiers = {
  lazy?: boolean;
  trim?: boolean;
  number?: boolean;
};
